get:
  tags:
    - Contacts
  summary: Get contact details
  description: Get contact details
  operationId: getContacts
  security: []
  responses:
    '200':
      description: successful operation
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/v1/contact.yml"
